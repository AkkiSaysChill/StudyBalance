# System Architecture & Data Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      USER INTERFACE (Frontend)                  │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  /ai-quiz    │  │  /routine    │  │ /activity-log│         │
│  │   (Quiz)     │  │  (Routine)   │  │ (Analytics)  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│        │                  │                  │                 │
│        └──────────────────┼──────────────────┘                 │
└──────────────────────────────────────────────────────────────────┘
                           ↓
                    HTTP/REST API
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                  BACKEND (Flask Application)                    │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           API Endpoints (app.py)                         │  │
│  │                                                          │  │
│  │  POST /ai-quiz                → Generate quiz           │  │
│  │  POST /save-quiz-results      → Save quiz to DB        │  │
│  │  POST /generate-routine       → Generate routine       │  │
│  │  POST /save-routine-to-db     → Save routine to DB     │  │
│  │  GET /get-activities          → Fetch all activities   │  │
│  │  GET /get-user-stats          → Fetch user statistics  │  │
│  └──────────────────────────────────────────────────────────┘  │
│        │                                              │         │
│        └──────────────────────┬───────────────────────┘         │
└──────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE (SQLite)                             │
│                  study_balance.db                              │
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  users   │  │ quizzes  │  │ routines │  │activities│      │
│  │          │  │          │  │          │  │          │      │
│  │ id       │  │ id       │  │ id       │  │ id       │      │
│  │ username │  │ user_id  │  │ user_id  │  │ user_id  │      │
│  │ email    │  │ topic    │  │ title    │  │ type     │      │
│  │          │  │ score    │  │ subjects │  │ quiz_id  │      │
│  │          │  │ questions│  │ schedule │  │routine_id│      │
│  │          │  │ answers  │  │ goals    │  │ created_at│     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Quiz Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ User visits /ai-quiz                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ↓
         ┌───────────────────────────┐
         │ Enters topic & # questions│
         │ Example: Math, 10 Qs      │
         └────────────┬──────────────┘
                      │
                      ↓
    ┌─────────────────────────────────────────┐
    │ Frontend sends: POST /ai-quiz          │
    │ {                                       │
    │   topic: "Math",                       │
    │   num_questions: 10                    │
    │ }                                       │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ Backend: generate_quiz_questions()      │
    │ Calls: OpenRouter API                  │
    │ Returns: 10 quiz questions             │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ Frontend displays quiz questions        │
    │ User answers all 10 questions           │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ User clicks: Submit Quiz               │
    │ Frontend calculates score: 85%         │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ Frontend AUTO-SENDS:                   │
    │ POST /save-quiz-results?user_id=1      │
    │ {                                       │
    │   topic: "Math",                       │
    │   totalQuestions: 10,                  │
    │   score: 8,                            │
    │   percentage: 85,                      │
    │   questions: [...],                    │
    │   userAnswers: {...}                   │
    │ }                                       │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ Backend: save_quiz_results()            │
    │ 1. Create Quiz record                  │
    │ 2. Save to 'quizzes' table             │
    │ 3. Create Activity record              │
    │ 4. Save to 'activities' table          │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ ✅ Database Updated                     │
    │ quizzes.id = 1                         │
    │ quizzes.topic = "Math"                 │
    │ quizzes.score = 8                      │
    │ quizzes.percentage = 85%               │
    │                                         │
    │ activities.id = 1                      │
    │ activities.type = "quiz"               │
    │ activities.quiz_id = 1                 │
    └────────────┬────────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────────┐
    │ Frontend shows: Results page            │
    │ "You scored 85%! Great job!"           │
    │ [Take Another Quiz]                    │
    └─────────────────────────────────────────┘
```

---

## 📋 Routine Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ User visits /routine                                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Fills in all preferences:            │
      │ - Wake time: 07:00                   │
      │ - Sleep time: 23:00                  │
      │ - Study duration: 45 min             │
      │ - Subjects: Math, Physics            │
      │ - Environment: quiet                 │
      │ - Preferences: active-recall, etc    │
      │ - Goals: Improve grades              │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Clicks: Generate My Routine          │
      │ Frontend sends: POST /generate-routine│
      │ with all preferences                 │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Backend: generate_study_routine()    │
      │ 1. Build AI prompt with preferences  │
      │ 2. Call OpenRouter API               │
      │ 3. Get personalized routine          │
      │ 4. Return 7-10 steps                 │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Frontend displays generated routine: │
      │ 1. 7:00 AM - Wake up                 │
      │ 2. 7:30 AM - Exercise 30 min         │
      │ 3. 8:00 AM - Math study 45 min       │
      │ ... (more steps)                     │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ User reviews routine                 │
      │ Clicks: 💾 Save to Database         │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Frontend sends:                      │
      │ POST /save-routine-to-db?user_id=1   │
      │ {                                    │
      │   routine: [steps...],               │
      │   preferences: {all data}            │
      │ }                                    │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Backend: save_routine_to_db()        │
      │ 1. Create Routine record             │
      │ 2. Save to 'routines' table          │
      │ 3. Create Activity record            │
      │ 4. Save to 'activities' table        │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ ✅ Database Updated                  │
      │ routines.id = 1                      │
      │ routines.wake_up_time = "07:00"      │
      │ routines.subjects = ["Math","Phys"]  │
      │ routines.routine_schedule = [...]    │
      │                                      │
      │ activities.id = 2                    │
      │ activities.type = "routine"          │
      │ activities.routine_id = 1            │
      └──────────────┬───────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Frontend shows:                      │
      │ "✅ Routine saved successfully!"     │
      │ [Create Another]                     │
      └──────────────────────────────────────┘
```

---

## 📊 Activity Log Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ User visits /activity-log                                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ↓
      ┌──────────────────────────────────────┐
      │ Page Load Event                      │
      └──────────────┬───────────────────────┘
                     │
    ┌────────────────┴────────────────┐
    │                                 │
    ↓                                 ↓
GET /get-user-stats?user_id=1    GET /get-activities?user_id=1
    │                                 │
    ↓                                 ↓
Backend queries database:         Backend queries database:
- Count quizzes                   - FROM activities
- Count routines                  - WHERE user_id=1
- Average score                   - JOIN with quizzes/routines
- Calculate stats                 - ORDER BY created_at DESC
    │                                 │
    ↓                                 ↓
Returns JSON:                     Returns JSON:
{                                 [
  total_quizzes: 2,              {
  total_routines: 1,              id: 1,
  average_score: 85.5,            type: "quiz",
  highest_score: 92,              title: "Math Quiz",
  ...                             quiz: {
}                                   score: 85,
    │                                ...
    ↓                                }
Update Stats Dashboard:           },
"Total Activities: 3"             {
"Quizzes: 2"                       id: 2,
"Routines: 1"                      type: "routine",
"Average: 85.5%"                   title: "Daily Routine",
    │                              routine: {
    └────────────┬─────────────────  subjects: ["Math"],
                 │                   ...
                 ↓                 }
        Display Activity List:    }
        ┌────────────────────┐    ]
        │ Quiz 1: Math (85%)  │       │
        │ Routine 1: Created │       │
        │ Quiz 2: Physics...│       │
        └────────────────────┘       │
                                     ↓
                          Initialize Charts:
                          - Bar chart (activities/day)
                          - Line chart (scores)
                          ↓
                          Display Overview Tab
                          ✅ Done!
```

---

## 🔄 Complete Data Journey

```
┌─ QUIZ COMPLETION ─┐
│                   │
│ User answers Qs   │
│ Submits Quiz      │
│ Gets Score: 85%   │
│                   │
└─────────┬─────────┘
          │
          ↓ AUTO-SAVE
          │
┌─────────────────────────────┐
│ Frontend sends score to      │
│ POST /save-quiz-results      │
└─────────┬───────────────────┘
          │
          ↓
┌─────────────────────────────┐
│ Backend processes:          │
│ 1. Parse data               │
│ 2. Create Quiz object       │
│ 3. Create Activity object   │
│ 4. Write to database        │
└─────────┬───────────────────┘
          │
          ↓
┌─────────────────────────────┐
│ DATABASE UPDATED:           │
│ - quizzes table (new row)   │
│ - activities table (new row)│
│ - study_balance.db (saved)  │
└─────────┬───────────────────┘
          │
          ↓
┌─────────────────────────────┐
│ User visits /activity-log   │
│ Page fetches data:          │
│ GET /get-activities         │
│ GET /get-user-stats         │
└─────────┬───────────────────┘
          │
          ↓
┌─────────────────────────────┐
│ Backend queries database:   │
│ SELECT * FROM activities    │
│ SELECT * FROM quizzes       │
│ Calculate stats             │
└─────────┬───────────────────┘
          │
          ↓
┌─────────────────────────────┐
│ Frontend receives data:     │
│ - List of all activities    │
│ - Statistics               │
│ - Quiz details             │
└─────────┬───────────────────┘
          │
          ↓
┌─────────────────────────────┐
│ Display:                    │
│ - Stats: "Quizzes: 2"       │
│ - List: "Math Quiz (85%)"   │
│ - Chart: Score trend        │
│ ✅ USER SEES THEIR DATA     │
└─────────────────────────────┘
```

---

## 🗂️ Database Relationships Diagram

```
users
├── has_many: quizzes
├── has_many: routines
└── has_many: activities

quizzes
├── belongs_to: user
├── has_one: activity (quiz_id)
└── contains:
    ├── topic
    ├── score
    ├── questions (JSON)
    ├── user_answers (JSON)
    └── completed_at

routines
├── belongs_to: user
├── has_one: activity (routine_id)
└── contains:
    ├── wake_up_time
    ├── sleep_time
    ├── subjects (JSON)
    ├── routine_schedule (JSON)
    └── created_at

activities
├── belongs_to: user
├── belongs_to: quiz (optional)
├── belongs_to: routine (optional)
└── contains:
    ├── activity_type ("quiz" | "routine")
    └── created_at
```

---

## 📱 API Endpoint Diagram

```
┌─────────────────────────────────────────────────────┐
│            QUIZ GENERATION ENDPOINTS                │
├─────────────────────────────────────────────────────┤
│                                                     │
│ POST /ai-quiz                                       │
│ ├─ Input: topic, num_questions                     │
│ └─ Output: array of questions                      │
│                                                     │
│ POST /save-quiz-results?user_id=1                   │
│ ├─ Input: quiz data (score, answers, etc)          │
│ └─ Output: {success, quiz_id}                      │
│                                                     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│           ROUTINE GENERATION ENDPOINTS              │
├─────────────────────────────────────────────────────┤
│                                                     │
│ POST /generate-routine                              │
│ ├─ Input: preferences (times, subjects, etc)       │
│ └─ Output: generated routine steps                 │
│                                                     │
│ POST /save-routine-to-db?user_id=1                  │
│ ├─ Input: routine data                             │
│ └─ Output: {success, routine_id}                   │
│                                                     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│            ACTIVITY & STATS ENDPOINTS               │
├─────────────────────────────────────────────────────┤
│                                                     │
│ GET /get-activities?user_id=1                       │
│ ├─ Input: user_id                                  │
│ └─ Output: array of all user activities            │
│                                                     │
│ GET /get-user-stats?user_id=1                       │
│ ├─ Input: user_id                                  │
│ └─ Output: {total_quizzes, average_score, ...}     │
│                                                     │
│ GET /activity-log                                   │
│ ├─ Input: none                                     │
│ └─ Output: HTML page with charts & data            │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

This covers all the diagrams showing how everything connects! 🚀
